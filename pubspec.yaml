name: fido2
description: Library to parse FIDO2 request / response and interactive with authenticators via CTAP2.
version: 2.0.0
repository: https://github.com/nfcim/fido2

environment:
  sdk: ">=3.4.0 <4.0.0"

dependencies:
  cbor: ^6.3.3
  convert: ^3.0.1

dev_dependencies:
  # Independent reference implementation for migration regression tests only.
  cryptography: ^2.7.0
  elliptic: ^0.3.11
  lints: ^4.0.0
  test: ^1.25.8
  dart_pcsc: ^2.0.2
